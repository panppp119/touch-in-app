import React from 'react'
import Logo from '../../Image/touch logo.png'
import './Logo.scss'

const logo = (props) => (
    <div className="Logo" >
        <img src={Logo} alt="logo" />
    </div>
)

export default logo
